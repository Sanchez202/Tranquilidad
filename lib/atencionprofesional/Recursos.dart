import 'package:flutter/material.dart';

class RecursosScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Recursos')),
      body: Center(child: Text('Contenido de Recursos')),
    );
  }
}