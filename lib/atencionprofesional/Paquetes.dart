import 'package:flutter/material.dart';

class PaquetesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Paquetes')),
      body: Center(child: Text('Contenido de Paquetes')),
    );
  }
}